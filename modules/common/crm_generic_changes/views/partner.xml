<?xml version="1.0"?>
<odoo>
    <!-- Add contextual button on partner form view for mail activities-->
    <record id="partner_form" model="ir.ui.view">
        <field name="name">Mail Activity Button</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button class="oe_stat_button" type="action"
                    name="%(mail.mail_activity_action)d"
                    icon="fa-star"
                    context="{'search_default_partner_id': id}" groups="sales_team.group_sale_salesman">
                    <field string="CRM Activity" name="activity_count" widget="statinfo"/>
                </button>
            </xpath>
            <xpath expr="//field[@name='category_id']" position="after">
                    <field name="rating"/>
            </xpath>
        </field>
    </record>


        <record id="view_partner_rating_tree" model="ir.ui.view">
            <field name="name">res.partner.rating.list</field>
            <field name="model">res.partner.rating</field>
            <field name="arch" type="xml">
                <list string="Partner Rating" editable="bottom">
                    <field name="name"/>
                </list>
            </field>
        </record>

</odoo>
