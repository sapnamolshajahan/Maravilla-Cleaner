<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record forcecreate="True" id="generate_actual_to_budget" model="ir.cron">
        <field name="name">Invoiced Compared to Budget</field>
        <field name="model_id" ref="model_sale_budget_dashboard"/>
        <field name="state">code</field>
        <field name="code">model.run_cron()</field>
        <field name="active" eval="True"/>
        <field name="user_id" ref="base.user_root"/>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
    </record>
</odoo>
