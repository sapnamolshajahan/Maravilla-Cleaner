<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- record id="account_forward_exchange_bank_statement_form" model="ir.ui.view">
        <field name="name">account.bank.statement.fec</field>
        <field name="model">account.bank.statement</field>
        <field name="inherit_id" ref="account.view_bank_statement_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='country_code']" position="after">
                    <field name="foreign_currency_account" invisible="1" />
            </xpath>
            <xpath expr="//field[@name='amount_currency']" position="after">
                <field name="fec_contract" groups="base.group_multi_currency"
                       attrs="{'invisible': [('parent.foreign_currency_account', '!=', True)]}"/>
                <field name="local_amount" groups="base.group_multi_currency"
                       attrs="{'invisible': [('parent.foreign_currency_account', '!=', True)]}"/>
            </xpath>
        </field>
    </record -->
    <!-- TODO - the new layout for bank statements is under enterprise -->
</odoo>