<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="account_journal_report_wizard" model="ir.ui.view">
        <field name="name">account.journal.report.wizard.view</field>
        <field name="model">account.journal.export</field>
        <field name="type">form</field>
        <field name="arch" type="xml">
            <form string="Selection Criteria">
                <field name="move_id" readonly="1"/>
                <group >
                    <field name="report_name" invisible="1"/>
                    <field name="data" filename="report_name"/>
                </group>
                <footer >
                    <button name="button_process" string="Create Report" type="object" class="oe_highlight"/>
                    <button special="cancel" string="Cancel" />
                </footer>
            </form>
        </field>
    </record>

    <record id="action_account_journal_report_wizard" model="ir.actions.act_window">
         <field name="name">Journal Export</field>
         <field name="res_model">account.journal.export</field>
         <field name="type">ir.actions.act_window</field>
         <field name="view_mode">form</field>
         <field name="view_id" ref="account_journal_report_wizard"/>
         <field name="context">{}</field>
         <field name="target">new</field>
   </record>

</odoo>

