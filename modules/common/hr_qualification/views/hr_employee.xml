<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="employee_form" model="ir.ui.view">
        <field name="name">Employee Form</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='work_information']" position="after">
                <page name="qualifications" string="Competencies">
                    <field name="qualifications" mode='list' nolabel="1">
                        <list editable="bottom">
                            <field name="qualification_id"/>
                            <field name="department_id"/>
                            <field name="status_id"/>
                            <field name="date_from"/>
                            <field name="date_expiry"/>
                            <field name="time_period"/>
                            <field name="is_expired"/>
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
