<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record model="ir.ui.view" id="account_trial_balance_wizard">
            <field name="name">account.trial.balance.report.wizard.view</field>
            <field name="model">account.trial.balance.export</field>
            <field name="type">form</field>
            <field name="arch" type="xml">
                <form string="Trial Balance">
                    <group>
                        <field name="as_at_date"/>
                        <field name="report_output"/>
                    </group>
                    <separator string="" colspan="4"/>
                    <group colspan="4">
                        <field name="report_name" invisible="1"/>
                        <field name="data" filename="report_name"/>
                    </group>
                    <footer>
                        <button special="cancel" string="Cancel"/>
                        <button name="button_process" string="Create File" type="object" default_focus="1"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_account_trial_balance_wizard" model="ir.actions.act_window">
            <field name="name">Account Trial Balance Report</field>
            <field name="res_model">account.trial.balance.export</field>
            <field name="type">ir.actions.act_window</field>
            <field name="view_mode">form</field>
            <field name="view_id" ref="account_trial_balance_wizard"/>
            <field name="context">{}</field>
            <field name="target">new</field>
        </record>


        <menuitem
                name="Trial Balance Report"
                action="action_account_trial_balance_wizard"
                id="menu_account_trial_balance_wizard"
                parent="account_generic_changes.account_reports_gl_menu"
                sequence="10"/>
    </data>
</odoo>

