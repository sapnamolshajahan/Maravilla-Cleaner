# Odoo Patches

This directory contains a list of local patches that should be applied
whenever a re-sync of the Odoo 18 is undertaken. All patches are
**git-diffs** that should be applied using `git apply` from the
top-level directory.
